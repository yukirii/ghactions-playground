name: release-build-tools

on:
  release:
    types:
      - published

env:
  DOCKER_BASE_NAME: docker.pkg.github.com/${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set short SHA
        run: echo "::set-env name=GITHUB_SHA_SHORT::$(echo ${GITHUB_SHA} | cut -c 1-8)"

      - name: Login to GHCR
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ env.GITHUB_ACTOR }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: |
            ${{ env.DOCKER_BASE_NAME }}:${{ github.event.release.tag_name }}
            ${{ env.DOCKER_BASE_NAME }}:${{ env.GITHUB_SHA_SHORT }}
            ${{ env.DOCKER_BASE_NAME }}:latest
